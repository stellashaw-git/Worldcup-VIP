import { AdminLeadsClient } from "@/components/admin/admin-leads-client";

export const metadata = {
  title: "Admin Leads | BLACKBOOK",
  robots: "noindex",
};

export default function AdminLeadsPage() {
  return <AdminLeadsClient />;
}
