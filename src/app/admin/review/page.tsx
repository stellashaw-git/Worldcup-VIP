import { AdminReviewClient } from "@/components/admin/admin-review-client";

export const metadata = {
  title: "Admin Review | BLACKBOOK",
  robots: "noindex",
};

export default function AdminReviewPage() {
  return <AdminReviewClient />;
}
