import { SubmitListingClient } from "@/components/submit/submit-listing-client";

export const metadata = {
  title: "Submit Listing | BLACKBOOK",
  description:
    "Submit hospitality, watch parties, hotels, and curated experiences for editorial review.",
};

export default function SubmitPage() {
  return <SubmitListingClient />;
}
